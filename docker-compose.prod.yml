services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - chroma_db:/app/chroma_db
    restart: unless-stopped

volumes:
  chroma_db:
